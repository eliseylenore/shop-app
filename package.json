{
  "name": "node-api",
  "main": "server.js",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "~1.0.1",
    "concurrently": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "is-empty": "^1.2.0",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.29.1",
    "mongoose": "^5.12.1",
    "passport": "^0.4.1",
    "passport-jwt": "^4.0.0",
    "stripe": "^8.176.0",
    "validator": "^13.5.2"
  },
  "scripts": {
    "serve": "node server.js",
    "start": "npm run serve",
    "start-dev": "concurrently -n 'server,client' -c 'blue,green' \"nodemon server.js\" \"npm --prefix ./client/ run serve\"",
    "heroku-postbuild": "cd client && npm run build && npm install"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
